# ============================================
# Database Configuration
# ============================================
# Supabase PostgreSQL connection string (or any PostgreSQL URL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/beton

# Environment: DEV or PROD
ENV=DEV

# Supabase (if using Supabase for database hosting)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-key

# ============================================
# DEPRECATED - Legacy Python Backend
# ============================================
# This file is for the legacy Python backend which is no longer used.
#
# For the current Next.js application, see:
#   frontend-nextjs/.env.local.example
#
# Integration credentials (PostHog, Attio, etc.) are now stored encrypted
# in the database and configured via the Settings UI.
