name: Preview Env Cleanup (Railway) - DISABLED

# DISABLED: Railway token handling needs fixes before re-enabling
# See: https://github.com/getbeton/inspector/issues
# Manual cleanup instructions: https://railway.app/docs/reference/cli

# Trigger disabled - uncomment 'on' section to re-enable
# on:
#   pull_request:
#     types: [closed]
#     branches:
#       - staging

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Preview environment cleanup temporarily disabled
        run: |
          echo "ℹ️  Preview environment cleanup is temporarily disabled."
          echo ""
          echo "Railway GitHub Actions integration has issues that need to be fixed."
          echo ""
          echo "Manual cleanup:"
          echo "  railway environment delete <env-name>"
          echo ""
          echo "To list all environments:"
          echo "  railway environment list"
