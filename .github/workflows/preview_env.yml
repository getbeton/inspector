name: Preview Env (Railway) - DISABLED

# DISABLED: Railway token handling needs fixes before re-enabling
# See: https://github.com/getbeton/inspector/issues
# Workaround: Use manual local deployment with scripts/railway_preview_env.sh
#
# To deploy a preview environment manually, use:
#   BRANCH_NAME=<branch> BASE_ENV_NAME=staging ./scripts/railway_preview_env.sh

# Trigger disabled - uncomment 'on' section to re-enable
# on:
#   push:
#     branches:
#       - "feature/**"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Preview environments temporarily disabled
        run: |
          echo "ℹ️  Preview environment automation is temporarily disabled."
          echo ""
          echo "Railway GitHub Actions integration has issues that need to be fixed."
          echo "See: https://github.com/getbeton/inspector/issues"
          echo ""
          echo "Workaround: Deploy preview environments manually using the local script"
          echo ""
          echo "Usage:"
          echo "  BRANCH_NAME=feature/my-branch BASE_ENV_NAME=staging ./scripts/railway_preview_env.sh"
          echo ""
          echo "Requirements:"
          echo "  - Railway CLI installed: https://railway.app/install"
          echo "  - Local authentication: railway login"
          echo "  - Or CI tokens: set RAILWAY_TOKEN and RAILWAY_PROJECT_ID"
